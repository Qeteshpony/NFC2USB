name: release-please

on:
  push:
    branches:
      - main

jobs:
  release-please:
    uses: release-please-workflow.yml@main
    with:
      config-file: release.kibot.yml
      board-file: pcb/NFC2USB.kicad_pcb
